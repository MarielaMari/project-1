export function getHint (getRandomMovie) {
  console.log (` Here is the hint:
        Genre: ${getHint.genre}
        Year: ${getHint.year}
        Director: ${getHint.director}
        Stars: ${getHint.stars}`);
}

getHint ();
